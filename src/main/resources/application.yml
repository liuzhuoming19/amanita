spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  profiles:
    active: dev

mybatis-plus:
  global-config:
    db-config:
      id-type: assign_uuid
  mapper-locations: classpath*:/mapper/*.xml

#自定义配置参数
custom:
  token-expire-time-amount: 24
  token-expire-time-unit: hours
  redirect-url: "https://futurenotfound.top/redirect?bookmarkId="
  user-bookmark-num-max: 50
  access-key-length: 32
  access-secret-length: 32
  access-expire-days: 90